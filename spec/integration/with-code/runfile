title 'The New Runfile'
summary 'Testing the new Runfile'

help    'Say something'
usage   'say MESSAGE...'
param   'MESSAGE', 'Message to say'
example 'run say hello world'
action 'say' do |args|
  dump args['MESSAGE']
end

# Defining (or requiring) any ruby code should work as well
def dump(text)
  pp text
end
