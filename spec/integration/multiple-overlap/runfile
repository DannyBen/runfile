title 'Overlap'
summary 'Testing overlapping commands between runfiles in the same directories'

import 'server'

action(:greet) { say "Greetings from rootfile" }
action(:stop) { say "Stop from rootfile" }
