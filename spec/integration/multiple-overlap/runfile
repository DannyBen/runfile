title 'Overlap'
summary 'Testing overlapping commands between runfiles in the same directories'

import 'server'

action(:greet) { say "Greetings from masterfile" }
action(:stop) { say "Stop from masterfile" }
