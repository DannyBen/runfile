The New Runfile

  Testing the new Runfile

Usage:
  run say MESSAGE
  run docker
  run [COMMAND] (--help | -h)

Commands:
  nb`docker`
    Run nu`run docker --help` for more information

Options:
  --help, -h
    Show this message