title   "Server"
summary "A Sample Server"

usage  "start [--daemon]"
action :start do |args|
  puts "Started in the "
  puts args['--daemon'] ? "background" : "foreground"
end

usage  "status <env>"
action :status do |args|
  puts "Status of #{args['<env>']} is A-OK"
end
